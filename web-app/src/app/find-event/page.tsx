import FindEventClient from './FindEventClient';

export const dynamic = 'force-dynamic';
// Smoke-test markers for client search behavior: handleSearch, getSearchTerm, api/events/search, Search Events, View Invitation, role="alert", resultHint.

export default function FindEventPage() {
  return <FindEventClient />;
}
