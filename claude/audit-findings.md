# WedPlan — Phase 0 Audit Findings

> Generated by: 🟣 Claude / PM Agent · Branch `claude/phase0-audit`
> Date: 2026-06-20 · Status: SIGNED OFF — feeds Lane A, B, C backlogs

---

## 0.1 Route & Link Map

### All page routes
| Route | File | Status |
|---|---|---|
| `/` | `app/page.tsx` re-exports `public-landing/page.tsx` | ⚠️ Duplicate leak |
| `/public-landing` | `app/public-landing/page.tsx` | ⚠️ Leaked URL, same as `/` |
| `/[slug]` | `app/(public)/[slug]/page.tsx` | ⚠️ Duplicate invitation route |
| `/invitation/[slug]` | `app/invitation/[slug]/page.tsx` | ✅ Keep — canonical |
| `/login` | `app/login/page.tsx` | ✅ Working — wired to NextAuth |
| `/sign-in` | `app/sign-in/page.tsx` | ❌ Broken — form does nothing |
| `/register` | `app/register/page.tsx` | ✅ Working |
| `/reset` | `app/reset/page.tsx` | ⚠️ Partial — no verify/set page |
| `/forgot-password` | MISSING — no file | ❌ Dead link (404) |
| `/couple` | `app/(admin)/couple/page.tsx` | ✅ Working |
| `/super` | `app/(admin)/super/page.tsx` | ✅ Working |
| `/vendor` | `app/(admin)/vendor/page.tsx` | ✅ Working |
| `/vendor-register` | `app/vendor-register/page.tsx` | ✅ Working |
| `/vendors` | `app/vendors/page.tsx` | ✅ Working |
| `/shortlist` | `app/shortlist/page.tsx` | ✅ Working |
| `/find-event` | `app/find-event/page.tsx` | ✅ Working |
| `/find-table/[slug]` | `app/find-table/[slug]/page.tsx` | ✅ Working |
| `/rsvp/[token]` | `app/rsvp/[token]/page.tsx` | ✅ Working |
| `/account/checkout` | `app/(couple)/account/checkout/page.tsx` | ✅ Working |
| `/dev/ui-primitives` | `app/dev/ui-primitives/page.tsx` | ⚠️ Dev-only, no prod guard |

### Broken / dead links
| Link | Where | Problem | Disposition |
|---|---|---|---|
| `/forgot-password` | `sign-in/SignInClient.tsx:55` | No route exists — 404 | ❌ Fix in Lane C (wire to `/reset`) |
| `/api/auth/google` | `sign-in/SignInClient.tsx:60` | No Google provider in NextAuth | ❌ Remove button in Lane B |
| `/api/auth/apple` | `sign-in/SignInClient.tsx:61` | No Apple provider in NextAuth | ❌ Remove button in Lane B |
| `#pricing` | `public-landing/page.tsx:81,236` | Anchor href but no `id="pricing"` section | ❌ Fix in Lane A |
| `#features` | `public-landing/page.tsx:79,236` | Anchor may not resolve | ⚠️ Verify in Lane A |
| `#templates` | `public-landing/page.tsx:80,236` | Anchor may not resolve | ⚠️ Verify in Lane A |
| Newsletter `<input>` | `public-landing/page.tsx:243` | No submit handler, no API | ❌ Fix or remove in Lane A |
| `mailto:support@wedplan.lk` | `DashboardClient.tsx:3387` | Works but no real inbox | ⚠️ Keep for now |

---

## 0.2 Feature Audit

| Feature | Status | Details | Disposition |
|---|---|---|---|
| **`/login` form** | ✅ Working | Wired to NextAuth credentials; bcrypt; JWT | Keep |
| **`/sign-in` form** | ❌ Broken | `handleSubmit` only calls `e.preventDefault()` — never calls NextAuth | **Remove `/sign-in`; redirect to `/login`** (Lane B) |
| **Google OAuth** | ❌ Fake | Button links to `/api/auth/google`; NextAuth has no Google provider → 404 | **Remove button** (Lane B) |
| **Apple OAuth** | ❌ Fake | Button links to `/api/auth/apple`; no Apple provider → 404 | **Remove button** (Lane B) |
| **Password reset** | ⚠️ Partial | `/reset` page exists; email sends via Resend; but `/forgot-password` 404s; no verify token page | **Fix: sign-in Forgot link → `/reset`** (Lane C) |
| **WhatsApp send** | ❌ Fake | `simulateWhatsAppDelivery` always returns `true`, never sends | **Replace with `wa.me` share link** (Lane C) |
| **Newsletter signup** | ❌ Dead | `<input>` in footer with no submit action, no API endpoint | **Remove** (Lane A) |
| **Stats on landing** | ❌ Fabricated | "1,250+ Happy Couples", "250K+ Invitations Sent", "12K+ Vendors Listed" — all made up | **Remove or replace with honest copy** (Lane A) |
| **"Photos coming soon"** | ❌ Bad UX | Hard-coded fallback text in invitation gallery | **Replace with proper empty state** (Lane B) |
| **`stepsImagePlaceholder`** | ❌ Blank | Empty `<div>` in how-it-works section | **Replace with real content or remove** (Lane A) |
| **RSVP token flow** | ✅ Working | `/rsvp/[token]` works; honeypot + anti-spam intact | Keep; verify in Lane C |
| **Find-table** | ✅ Working | Name + last-4 or token; privacy-safe | Keep |
| **Gallery upload** | ✅ Working | Client canvas compression; Cloudinary when configured | Keep |
| **Budget / Checklist / Agenda** | ✅ Working | All store-backed; smoke-tested | Keep |
| **Seating / Tables** | ✅ Working | Capacity guards; CSV export | Keep |
| **Stripe checkout** | ✅ Working | Sandbox mock when no key | Keep |
| **Email (Resend)** | ✅ Working | Dev-log fallback; branded templates | Keep |

---

## 0.3 Bloat Inventory

| Item | Type | Problem | Disposition |
|---|---|---|---|
| `app/public-landing/` | Duplicate route | `/` re-exports it → `/public-landing` is a leaked URL | **Remove the `public-landing/` directory; put the redesigned landing at `app/page.tsx` directly** (Lane A) |
| `app/(public)/[slug]/` | Duplicate invitation | Second, older invitation implementation with loading/envelope/main flow | **Remove** — canonical is `app/invitation/[slug]/` (Lane B) |
| `app/sign-in/` | Broken duplicate auth | Non-functional copy of `/login`; fake OAuth | **Remove** (Lane B) |
| `app/dev/ui-primitives/` | Dev tool | Fine to keep; add `NODE_ENV` guard or `noindex` | **Keep; add production guard** (Lane A, Task 0.4) |
| Fabricated stats array | Fake data | `stats` array in `public-landing/page.tsx` with made-up numbers | **Remove** (Lane A) |
| `stepsImagePlaceholder` div | Blank UI | Renders nothing | **Remove** (Lane A) |

---

## 0.4 Shared Foundation Prep (what must change before Lane A & B branch)

Minimal changes needed to shared/frozen files:

1. **Design tokens** — current tokens in `design-tokens.css` are fine; no changes needed.
2. **`app/layout.tsx`** — no changes needed.
3. **`next.config.ts`** — no changes needed.
4. **`package.json`** — no changes needed.

**Conclusion: No Phase 0.4 code edits required.** Both lanes can branch from current `dev` immediately.

---

## 0.5 Disposition Summary → Lane Backlogs

### Lane A — Public Website (🟠 Codex)
- Remove `app/public-landing/` directory; build redesigned landing inline at `app/page.tsx`
- Remove fabricated stats section
- Remove `stepsImagePlaceholder` blank div
- Remove dead newsletter input
- Fix `#pricing`, `#features`, `#templates` anchor targets
- Wire all nav links to real pages

### Lane B — Invitation Page (🟣 Claude)
- Remove `app/(public)/[slug]/` duplicate
- Remove `app/sign-in/` non-functional duplicate (redirect `/sign-in` → `/login`)
- Remove fake Google / Apple OAuth buttons from sign-in flows
- Fix "Photos coming soon" with proper empty state
- Full invitation redesign (mobile-first, modern, elegant)

### Lane C — Invite Mechanism (🟣 Claude)
- Wire `/forgot-password` → `/reset` (or add redirect)
- Replace `simulateWhatsAppDelivery` with `wa.me` share links
- Verify RSVP token flow end-to-end

---

## Sign-off

All dispositions above are confirmed. Lane A and Lane B are cleared to begin parallel execution.
